using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Text;
using UnityEngine.UI;
using System.IO;
using System.Threading;

public class DestinationMessage : MonoBehaviour
{
    // Start is called before the first frame update
    private Text smartphone;
    private string Destination;
    
    void Start()
    {
    //    smartphone = GameObject.FindGameObjectsWithTag("smartphone").GetComponent<Text>();

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
